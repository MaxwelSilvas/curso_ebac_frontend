function multiplicaNumeros(numeroA: number, numeroB: number): number {
    return numeroA * numeroB;
}

const resultadoMutiplicacao = multiplicaNumeros(10, 5);
console.log(resultadoMutiplicacao);

function saudacao(nome: string): string {
    return `Olá ${nome}`;
}

const resultadoNome = saudacao("Maxwel");
console.log(resultadoNome);
